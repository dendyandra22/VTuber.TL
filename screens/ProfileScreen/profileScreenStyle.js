import { StyleSheet, Dimensions, Platform } from "react-native";

export default StyleSheet.create({
    container: {
        shadowColor: 'black',
        height: 170, 
        width: 370,
        shadowOpacity : 0.5,
        shadowRadius: 3,    
        margin: 8
        // borderWidth: 2
    }
});